"""
Pure algorithms with no domain-specific dependencies.

Modules:
    cliques     - K-reciprocal nearest neighbor clique finding
    graph       - Connected component extraction
    tree        - Tree traversal and transformation
    dag         - DAG operations (topological sort)
    union_find  - Union-Find (disjoint set) data structure
    sets        - Set operations
"""
